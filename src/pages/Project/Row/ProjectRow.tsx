import React, { ReactElement } from "react";
// Styles
import "./style.css"

const ProjectRow = (): ReactElement => (
  <div className="project-row">
    <h1>Project Title</h1>
  </div>
);

export default ProjectRow;
